# Лабораторная работа №2

### Overview
* **Дата:** 06.03.2026
* **Тема:** Основы NumPy: массивы и векторные операции
* **Статус:** [Completed]

---

### Objective
Целью работы является освоение навыков работы с многомерными массивами **NumPy**, реализация матричных операций, проведение базового статистического анализа данных и визуализация результатов с помощью **Matplotlib** и **Seaborn**.

### Implementation
В ходе работы был реализован модуль `main.py`, включающий следующие группы функций:

1.  **Создание и трансформация массивов:** использование `np.arange`, `np.random.rand` и `vec.reshape` для подготовки данных.
2.  **Векторные и матричные операции:** реализация поэлементного сложения, скалярного произведения (`np.dot`), умножения матриц (`@`) и решение систем линейных уравнений через `np.linalg.solve`.
3.  **Анализ данных:** загрузка данных из CSV с помощью `pd.read_csv().to_numpy()` и расчет статистик (среднее, медиана, перцентили) через встроенные методы NumPy.
4.  **Визуализация:** построение гистограмм распределения, тепловых карт корреляции и линейных графиков успеваемости. Для корректной работы в серверных средах и автоматических тестах использован бэкенд `matplotlib.use('Agg')`.

### Conclusion
В результате выполнения работы были изучены принципы **векторизации вычислений**, что позволяет обрабатывать данные без использования медленных циклов Python. Успешное прохождение 17 юнит-тестов подтверждает корректность работы всех математических и статистических алгоритмов. Все сгенерированные графики сохранены в директорию `plots/` для дальнейшего анализа.

### Запуск проекта
1. Активируйте окружение: `source numpy_env/bin/activate` (или `Scripts\activate` на Windows)
2. Запустите тесты: `python -m pytest tests.py -v`
3. Сгенерируйте отчет: `python main.py`

### Code
```python
def hello_world():
    print("Lab 2 completed")